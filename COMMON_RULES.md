# 共通開発ルール（COMMON RULES）

**適用対象**: Kokkai Nexus / PoliDATA 系全リポジトリ
**版**: v1.8（2026-09-08）
**位置づけ**: 本ファイルは全リポジトリの上位規範である。各リポジトリの `CLAUDE.md`・実装・設計判断は本ファイルに従う。本ファイルと個別ドキュメントが矛盾した場合、本ファイルが優先する。

---

## 0. 本ファイルの運用ルール

### 0-1. 配置と配布

| 項目 | 規定 |
|---|---|
| 正本の置き場所 | 独立リポジトリ `kokkai-nexus-rules`（新設）。`kokkai-nexus-master` には**含めない** |
| 各リポからの参照 | Kokkai Nexus 系・PoliData 系の**すべてのリポジトリ**が Git Submodule として `kokkai-nexus-rules` を参照する。**コピーを作らない**（分岐して腐るため） |
| 版の固定 | 各リポの Submodule 参照は、本ファイル §12 の版に対応する `git tag`（`v1.0` / `v1.1` / `v1.2` …）に**ピン留めする**。HEAD への浮動参照は MUST NOT（他リポが未検証の変更を無自覚に取り込むため） |
| 収録物 | `COMMON_RULES.md`（完全版）、`CLAUDE_CORE.md`（AIエージェント用ダイジェスト、§9-4）、`CLAUDE.md.template`、`document_catalog.yaml` の雛形、`entity_master.json` の雛形、および ID・trust_layer 等の JSON Schema 群を同居させる |
| 各リポの `CLAUDE.md` | 冒頭に「本リポは `COMMON_RULES.md` に従う」と明記し、**差分のみ**を記述する |

**決定理由（ADR-0002 相当）**: `kokkai-nexus-master` を Submodule として PoliData 系に取り込むと、市民向け本体固有のデータ・依存関係まで巻き込む「重量オーバー」になる。規約・マスタスキーマのみを持つ専用リポジトリを共通の親として切り出すことで、Kokkai Nexus 系・PoliData 系の両方が対等かつ軽量に参照できる。

### 0-2. 用語の強度（RFC 2119 準拠）

- **MUST / してはならない** — 違反は設計上の欠陥。ADR なしに逸脱禁止。
- **SHOULD / 望ましい** — 逸脱時は理由をコード上または PR に記す。
- **MAY / 任意** — 判断に委ねる。

### 0-3. 変更手順

本ファイルの §1（不変原則）を変更する場合は、`docs/decisions/ADR-NNNN-*.md` を起票し、変更理由・却下した代替案・影響範囲を記録すること。§2 以降は PR で変更可とするが、変更履歴（§12）への追記を MUST とする。

---

## 1. 不変原則（Immutable Principles）

本章は事業の存在理由に直結する。実装上の都合で曲げてはならない。

### P1. 記録者の独立性

記録対象（政治家・政党・官公庁）および広告主は、記録の内容・修正・削除に介入する権限を持たない。法令に基づく請求のみを例外とする。
実装への含意: 記録対象からの申告を反映する経路は「修正の自動適用」ではなく「異議の注記（`objection_note`）」として設計する。

### P2. 非削除（Non-Deletion）

物理削除（`DELETE`）を MUST NOT とする。すべての除去は以下で表現する。

- 非表示: `is_hidden` / `visibility` フラグ
- 状態遷移: `missing` → `archived`（消滅を「消滅した事実」として記録する）
- 監査: 変更は必ず `*_changes` / audit trigger に残す

削除が正当化されるのは、法令上の義務がある場合のみ。その場合も削除の事実・理由・日時を記録し開示する。

### P3. 完全な検証可能性（Zero-Trust AI）

LLM が出力するすべての回答は、一次ソースへ 100% 逆引き可能でなければならない。

- 逆引きの単位は「記事URL」ではなく **実体**（省庁PDFのバイナリ、字幕のタイムコード、YouTube のタイムスタンプ付きURL、写真のbbox）
- 逆引きできない情報は**出力しない**。「たぶん正しい」は本システムでは無価値である。

### P4. 事実と表現の分離

第三者の著作物は、**事実（fact）を構造化して保持**し、**表現（expression）を再配布しない**。

- 報道記事: 見出し＋リンク＋自前の要約のみ。本文転載 MUST NOT
- 議員SNS: 要約＋出典リンクのみ。原文転載 MUST NOT
- Bronze層の原本保持は「内部の検証用アーカイブ」であり、配信対象ではない（§4-1）

### P5. LLM は入力補助、確定は記名の人間

LLM 出力を無確認で確定データへ昇格させることを MUST NOT とする。

- AI 生成値は必ず `is_ai_generated` / `confidence` を持つ
- Layer 4（取材・注釈）への昇格は、記名された人間の承認を経る
- UI 上、AI案と確認済みを視覚的に分離する（黄＝AI案 / 緑＝確認済み）

### P6. 影響力行使支援の忌避

「訪問ルート」「キーパーソン攻略」等、ロビイング支援に directional に近づく機能は実装しない。本サービスは**記録と検証**の側に立つ。企業向け販売（PoliData）においても、提供物は「検証可能なインテリジェンス」であり「働きかけの手順」ではない。

### P7. 後世への責任

記録の採否基準は「現在の権力にとって都合がよいか」ではなく「後世の歴史的検証に必要か」で判断する。

---

## 2. 識別子（ID）規約

### 2-1. 発番の単一化（MUST）

同一種のIDを複数システムが独立に発番してはならない。発番権限は以下に固定する。

| ID | 形式 | 発番元（唯一） | 意味 |
|---|---|---|---|
| `person_id` | `P000001` | Supabase `polidata-db`（人物マスタ） | 議員・候補者・官僚・その他人物の統一ID |
| `event_id` | `EV-<YYYYMMDD>-<4桁連番>`（例: `EV-20260724-0042`） | Supabase `events` テーブル | 事象を束ねる「血管」。Web/PDF/TV/YT の全ソースを串刺す |
| `theme_id` | `TH-<SLUG>-<3桁連番>`（例: `TH-DEFENSE-001`, `TH-SHUIN2026-002`） | Supabase（テーマ管理） | 予算編成・総裁選など長期政局を束ねる親ID |
| `source_id` | UUID | 各コレクタ | 取得元（媒体・巡回対象）単位 |
| `snapshot_id` | UUID | 各コレクタ | 個々の取得原本（SHA256 を保持） |

> **重要**: GCS / Pinecone / 各パイプラインは `event_id` を**参照する側**であり、発番してはならない。Pinecone の metadata には `event_id` / `theme_id` / `person_id` を必ず焼き付ける。

**a. ID形式の運用上の注意（MUST）**

- ID文字列に日付を埋め込むのは**人間が目視・デバッグする際の可読性のため**であり、時系列フィルタリングを ID 文字列のパース（例: 先頭8文字を日付として切り出す）で行うことを MUST NOT とする。フィルタリングは必ず構造化された日付カラム（`occurred_on` 等）を用いる。ID はラベル、フィルタ条件は別カラム、と役割を分離する。
- 日付単位の連番（`event_id` の4桁部分）は、複数プロセスの同時書き込みで衝突し得る。採番は Postgres 側でアトミックに行う（例: `event_seq`（`seq_date`, `last_number`）テーブルに対する `SELECT ... FOR UPDATE`、またはPostgresシーケンスの日次リセット運用）。アプリケーション側での「SELECT最大値+1」による採番は競合状態を生むため MUST NOT。

**b. `theme_id` のスラッグ管理**

- スラッグ部分は英大文字・数字・ハイフンのみに正規化する（例: `DEFENSE`, `SHUIN2026`）。日本語や表記揺れ（`BOUEI` と `DEFENSE` 等）が並立しないよう、`theme_slug_registry`（Supabase内の一覧テーブルまたは `kokkai-nexus-rules` 側のマスタ）で一元管理する。
- 新規テーマ作成時は、既存スラッグとのあいまい一致チェック（表記揺れ・同義語）を経て、**人間が最終確認**したうえで登録する。自動生成のみでの新規スラッグ発行は MUST NOT。

**c. 外部システムの安定キー（MUST・ADR-0004）**

外部システム（別DB・別パイプライン）が独自のIDでレコードを持つ場合、そのIDを本体テーブルの外部キーに直接使ってはならない。`<system>_external_ids` 形式の対応表で受ける。

```sql
create table event_external_ids (
  event_id    text not null references events(event_id),
  system      text not null,          -- 例: 'polidata_schedule_sqlite'
  external_id text not null,          -- 決定論的に再現できる安定キー
  external_pk text,                   -- 相手システムの内部ID（監査用）
  synced_at   timestamptz not null default now(),
  primary key (system, external_id)
);
```

- `external_id` には、**再実行しても同じ入力から同じ値が得られる決定論的なキー**を用いる（MUST）。連番・自動採番・挿入順に依存する値を用いることを MUST NOT とする
- 相手システムの内部ID（連番等）は `external_pk` に監査用として保持してよいが、**結合条件に使わない**（§2-1a と同じ精神）
- `primary key (system, external_id)` により、同じ外部レコードが二重にIDを発番されることを防ぐ

> 理由: 冪等な再構築（`DROP TABLE` して作り直す設計）を行うパイプラインでは、連番IDは実行のたびに別のレコードを指しうる。例外もエラーも出ず、データだけが静かにずれるため発見が極めて遅れる。

### 2-2. 命名の一貫性

- 人物名の表記揺れは `entity_master.json`（時系列マスタ）で解決する。「高市大臣」「高市早苗」は同一 `person_id` に解決される。
- 同名異人は `nameOfHouse`（院）・当選年・選挙区で判別する。自動解決不能な場合は `pending` として人間へ回す。
- 政党の改称・合流・分裂は「上書き」ではなく**期間付きレコード**で表現する（`valid_from` / `valid_to`）。

---

## 3. データレイヤー規約

### 3-1. 三つの軸を混同しない（MUST）

現在、1〜5 の数字を使う概念が三つ並存しており、混乱の最大の原因である。**必ず接頭辞を付けて記述する**。

| 軸 | 表記 | 意味 | 値域 |
|---|---|---|---|
| 信頼性レイヤー | `trust_layer` | 情報の出所と取得方法の分類 | L1〜L5 |
| 信頼度スコア | `trust_score` | ソース単位の信頼性ガードレール | 1〜5 |
| 加工段階 | `medallion` | データレイク上の処理段階 | bronze / silver / gold |

コード・DB・プロンプト中で裸の `layer: 3` のような記述を MUST NOT とする。`trust_layer: "L3"` と書く。

### 3-2. trust_layer 定義（正本）

| Layer | 分類 | 内容例 | 著作権上の扱い |
|---|---|---|---|
| L1 | 公開・静的 | 国会会議録、選挙結果、政治資金収支報告書、官報 | 著作権法13条により権利なし |
| L2 | 公開・動的 | 議員SNS、政党発表、報道記事見出し | 引用の範囲内。本文は原則リンクのみ |
| L3 | 独自構造化 | ネットワーク分析、資金フロー、AI推論タグ | 運営者に帰属 |
| L4 | 取材・注釈 | 党内序列、実質的影響力（記名・出典明示） | 取材者に帰属。運営者が利用許諾を取得 |
| L5 | 本人証言 | 手記・回顧録・インタビュー（書誌情報付き） | 引用の範囲内 |

### 3-3. trust_score 対応表

| Score | 対象 |
|---|---|
| 5 | 憲法・国会法・先例集・官報・官邸公式・国会会議録 |
| 4 | 政党一次情報、省庁公式PDF、選管確定値 |
| 3 | 一般報道、テレビ字幕（生字幕は誤変換前提） |
| 2 | 本人SNS（自己演出前提）、匿名を含む二次的言及 |
| 1 | 未検証・要確認 |

### 3-4. L4 / L5 の掲載基準（MUST・全項充足）

1. 取材者の氏名または識別符号が明示されている
2. 独立した複数の証言、または客観的な裏付けが存在する
3. 情報の性質（取材由来・証言ベース等）と確認状況が記録されている
4. 公職者の**公的活動**に関する事項である
5. 「評価」ではなく「観測記録」として記述する。**数値スコアへの変換 MUST NOT**（「影響力スコア85点」は禁止例）

匿名の情報提供は、Human-in-the-Loop エキスパート回答を含め**自動破棄**する（記名・専門性検証可能なもののみ採用）。

### 3-5. アクセス制御

- L1 / L2 / L3: 公開可
- **L4 / L5: 要ログイン**（本プラットフォームの最大の防御対象。スクレイピング対策の優先順位は L4/L5 > L3 > L1/L2）

### 3-6. L4データの二系統ブランド間流用（Kokkai Nexus / PoliData）

L4（記名取材・観測記録）の正本は単一のデータベースで保持し、Kokkai Nexus（市民向け）と PoliData（商用）は**同一データを参照する**。ただし商用側（PoliData）での利用可否は、データ層で強制する（**アプリケーションのクエリ条件だけに頼ることを MUST NOT**）。

**a. 同意フラグとデフォルト**

```yaml
commercial_use_allowed: false   # 既定値は false（未同意）。true への変更は明示的な同意取得後のみ
consent_recorded_at: null       # 同意取得日時
consent_document_id: null       # 同意書・許諾契約の参照ID
```

- 取材者（記名取材者）の著作権は本人に帰属する（利用規約 第6条4項 相当）。よって**明示的な許諾が記録されるまでは商用利用不可がデフォルト**である。「特に反対がなければ許諾とみなす」という運用は MUST NOT。
- 許諾は全件一括ではなく、取材者ごと／案件ごとに記録する。取材者が「この観測記録は市民向け記録目的に限る」と明示した場合、その意思を上書きしない。

**b. DB層での強制（RLS必須・MUST）**

PoliData（商用側）が参照するロール（例: `polidata_readonly`）に対し、Supabase の Row Level Security で以下を強制する。

```sql
create policy polidata_l4_commercial_only
on layer4_annotations
for select
to polidata_readonly
using (commercial_use_allowed = true);
```

アプリケーションコード側の `WHERE commercial_use_allowed = true` は補助的なものとし、**DBロールレベルのRLSを一次防御線とする**。理由: アプリ層のフィルタ漏れ（実装ミス・レビュー漏れ）は起こり得るが、RLSはロールに紐づくため実装ミスに依らず機能する。

**c. アウトプット側の制約（P6 との整合）**

PoliData側が提供する分析（政策決定プロセスの構造分析・リスク予測・過去質疑の横断検索）は、「特定議員への働きかけ手順」の生成を **プロンプト制約だけでなく、機能・スキーマのレベルで実装しない**（P6）。プロンプトガードレールのみに依存する設計は MUST NOT（迂回され得るため）。「訪問ルート」等の機能は、そもそも API・UIに存在しない状態を維持する。

**d. 出所の可視性**

商用側で L4 データを利用する場合も、そのデータが Kokkai Nexus の独立した取材由来である旨（記録者独立性 P1）の出所表示を省略しない。ブランドをまたいでも「誰が何を根拠に記録したか」の透明性は維持する（§3-3）。

> **本節は事業モデルに関わる決定を含むため、Souichirou の最終確認を経てから確定とする。** 特に (a) のデフォルト値を `false` とする点は、提示案（デフォルト許諾）からの変更である。

---

## 4. ストレージ責務分担

### 4-1. メダリオン構成（データレイク = GCS 単一バケット）

| 層 | 内容 | 保持方針 |
|---|---|---|
| **Bronze** | 生HTML、PDF実体、生字幕、音声ストリーム、スクリーンショット | 月単位・ソース別に `/raw` へ**永久保持**。リンク切れ・アーカイブ削除への保険。改変 MUST NOT（Immutable） |
| **Silver** | ノイズ除去済み Markdown ＋ YAML Front Matter、メタデータJSON | `/silver`。政治ドメイン知識に基づく整形（フィラー・誤変換除去） |
| **Gold** | 完全構造化データ | Supabase / Pinecone へのインジェスト用 |

開発環境からは `gcsfuse` でマウントし、ローカル `data/` として透過的に扱う。

### 4-2. サービング層の役割分担（MUST）

| 用途 | 格納先 | 理由 |
|---|---|---|
| 日程・カレンダー・法案ステータス・確定データの正本 | **Supabase (RDB)** | 条件絞り込み・アラート発火など決定的処理 |
| 議事録・字幕・発言録の意味検索 | **Pinecone (Vector)** | 文脈・ニュアンス検索（RAG） |
| 検証済みマスタデータ | `kokkai-nexus-master`（Git Submodule） | 版管理・差分レビューが必要なもの |
| 収集直後の名寄せ・重複統合の作業場 | **SQLite（各収集リポ内）** | 冪等な再構築（毎回作り直す）が前提の中間処理。正本ではない（§4-2a） |

**原則**: 「答え合わせができる事実」は RDB、「言葉のニュアンス」は Vector。どちらに置くか迷ったら RDB を正本とし、Vector は派生とみなす（Vector は再構築可能でなければならない）。

**a. 収集リポ内 SQLite の位置づけ（ADR-0004）**

収集リポジトリが内部に持つ SQLite は、**Bronze と Silver の間の名寄せ・重複統合の作業場**であり、`event_id` 等の共通IDを発番する場所ではない（§2-1）。外部への公開は Datasette 等による読み取り配信に限り、他システムはその内部IDを参照しない。Supabase への昇格は §2-1c の対応表を経由する。

### 4-3. 接続方式

- Supabase へは `@supabase/supabase-js` を用いる。**`pg` による直接接続を MUST NOT**（IPv6 起因の不安定性）
- マイグレーションは Drizzle ORM または `migration_*.sql` を用い、必ずリポジトリに残す。本番SQLエディタでの直接変更 MUST NOT

### 4-4. 物理バックアップ（事業継続計画・BCP）

放送録画（TS）・スクリーンショット等、**再取得不可能な一次資産**（一度放送・削除されると二度と同じ形では入手できないもの）については、クラウド保管のみに依存せず 3-2-1 ルールを適用する（MUST、特に tclip / TVエッジのTSデータに適用）。

- **3つのコピー**: ① GCS本番データ、② オフィスの物理NAS/PCコピー、③ 取得エッジ（TVチューナー等）のローカル一時キャッシュ
- **2つの異なる媒体**: クラウド（オブジェクトストレージ）と物理（HDD/SSD）
- **1つは遠隔地**: クラウド（データセンター）と物理拠点（オフィス）を地理的に分離する

自動 rsync による一方向差分同期（`gcloud storage rsync` 等）を深夜バッチで実行し、ローカル側の誤削除が本番（GCS）に影響しない非対称設計とする。クラウドアカウントの凍結・BAN・サイバー攻撃への備えとして、この物理バックアップ戦略は本番稼働の最初期から組み込む。

---

## 4a. Bronze 取得契約

> **P2/P3 実装規定**: 本節は新たな原則ではない。§1-P2（非削除）および §1-P3（完全な検証可能性）を、外部データ取得の境界面において具体化した実装規定である。§5系の全パイプライン規定が本契約に依存するため、参照の向きが逆転しないようこの位置に配置する。

契約バージョン: **1.0**
発効日: 2026-08-03

### 4a-1. 位置づけ

本節は新たな原則ではない。**P2（非削除）および P3（全検証可能性）を、外部データ取得の境界面において具体化した実装規定**である。

原則が「記録を消さない」「全て検証可能にする」と述べるのに対し、本節は「取得の瞬間にしか記録できない事実を、取得の瞬間に必ず記録する」ことを定める。

上位層（Silver / Gold / Assertion / State）の設計は今後変更されうる。しかし本契約が定める記録は、**一度取り損ねると事後に復元する手段が存在しない**。したがって本契約は、上位設計の確定を待たずに発効する。

### 4a-2. 適用範囲

外部から取得する全データに例外なく適用する。以下を含む。

- HTTP による取得（スクレイピング、API 呼び出し）
- ブラウザ自動操作による取得
- 手動でダウンロード・配置したファイル（CSV、ICS、PDF 等）
- 物理機器による取得（テレビチューナー、録画機器）
- 第三者から提供・購入したデータ（データベース契約による一括取得を含む）

「手で落としたファイルだから対象外」という運用は認めない。手動取得の場合も、取得者が `fetch_record` を作成する。

### 4a-3. 必須要件

以下 R1〜R5 を全て満たさない収集器は、本番パイプラインに追加してはならない。

#### R1. 取得単位への不透明ID

取得の1回ごとに `fetch_id`（UUIDv7）を発番する。

- **主キーは不透明であること。** 日付・ソース名・分類を埋め込んだIDを主キーにしない
- 人間可読な識別子が必要な場合は `display_id` を別カラムとして持つ。`display_id` は可変とし、一意制約を課さない
- `fetch_id` は発番後、いかなる理由でも変更しない

**理由**: IDに埋め込んだ意味は、後に訂正されうる。取得日を埋め込んだIDは、取得時刻の誤記が判明した時点で嘘になる。IDが嘘をつくか、IDを変更して P2 に反するかの二択を作ってはならない。

#### R2. 取得文脈の記録

各 `fetch_record` は以下を保持する。

| 項目 | 必須 | 備考 |
|---|---|---|
| `requested_url` | ○ | 実際にリクエストしたURL |
| `final_url` | ○ | リダイレクト後の最終URL。リダイレクトが無い場合も記録 |
| `redirect_chain` | ○ | リダイレクトが無い場合は空配列。`null` にしない |
| `http_method` | ○ | |
| `http_status` | ○ | |
| `response_headers` | ○ | 全ヘッダ。`Last-Modified` / `ETag` / `Content-Type` を含む |
| `request_headers_sent` | ○ | 送信した `User-Agent` 等。認証情報は除外し、キー名のみ記録 |
| `fetched_at` | ○ | RFC 3339、**タイムゾーンオフセット必須**。`Z` または `+09:00` |
| `collector_id` | ○ | |
| `collector_version` | ○ | git commit hash または semver |
| `bronze_contract_version` | ○ | 本契約のバージョン |

**理由**: サーバーがその瞬間に何を返したかは、後から一切復元できない。`Last-Modified` は原本の更新検知に、`redirect_chain` は URL 体系変更の追跡に、`collector_version` は抽出結果の再現に、それぞれ後年必ず必要になる。

タイムゾーンを省略した時刻は記録として無効とする。40年分の履歴を扱う以上、ローカル時刻の暗黙前提は破綻する。

#### R3. デコード前バイト列とハッシュ

取得したバイト列を、**一切の加工を加えずに**保存する。

- 文字コード変換をしない
- 改行コードを正規化しない
- HTML の整形・タグ除去をしない
- PDF からのテキスト抽出結果で原本を代替しない
- 圧縮は可（保存形式の問題であり、内容の変更ではない）

保存と同時に `sha256` を計算し `content_hash` に記録する。`content_length` も記録する。

**理由**: 文字コード判定も PDF のテキスト層抽出も OCR も、ツールとバージョンによって結果が変わる。原本のバイト列が残っていれば何度でもやり直せるが、抽出結果しか残っていなければ永久に手詰まりになる。Silver 層が「再生成可能」であるという定義は、Bronze 層に原本が存在することによってのみ成立する。

#### R4. 権利状態のスナップショット

対象データの取得時に、以下を**同時に**取得・保存する。

| 項目 | 内容 |
|---|---|
| `robots_txt_fetch_id` | 当該ドメインの `robots.txt` を取得した `fetch_record` への参照 |
| `robots_allows` | 判定結果（true / false / null）と判定に用いた User-Agent |
| `terms_fetch_id` | 利用規約ページを取得した `fetch_record` への参照。存在しない場合 `null` |
| `license_declared` | ページまたはAPIが明示するライセンス表記。無い場合 `null` |
| `rights_checked_at` | 上記の確認時刻 |

`robots.txt` と利用規約の再取得は、同一ドメインにつき **7日以内のキャッシュを再利用してよい**。ただし参照する `fetch_id` は必ず記録する。

**理由**: 数年後に「この取得は許諾範囲内だったか」を検証する必要が必ず生じる。そのとき対象サイトの規約は書き換えられており、当時の規約は失われている。当時の規約を当時のバイト列として保持していることが、唯一の防御手段となる。

この要件は4項目のうち最も見落とされやすく、最も遅れて効く。省略してはならない。

#### R5. 取得できなかったことの記録

欠損は**既知の欠損**でなければならない。2つの粒度で記録する。

**(a) 試行単位の失敗** ── `fetch_record` を通常どおり作成し、`outcome` に結果を記録する。

```
success | not_found | forbidden | rate_limited | timeout
| network_error | content_changed_unexpectedly | skipped_by_rule
```

失敗時もレコードを作成する。作成しないことは禁止する。`failure_detail` に具体的な事由を記録する。

**(b) 期間単位の非巡回** ── そもそも実行されなかった期間を `coverage_log` に記録する。

```
collector_id / window_start / window_end
/ expected_runs / actual_runs / gap_reason
```

`gap_reason` の例: `not_yet_implemented` / `infrastructure_outage` / `source_unavailable` / `intentionally_paused` / `unknown`

**理由**: 「網羅している」ことを商品価値とする以上、データが存在しない理由が「元々無かった」のか「取り損ねた」のかを判別できなければならない。これが判別できない記録は、網羅性を主張できない。

`gap_reason: unknown` は許容する。**遡って理由を捏造するよりは、不明と記録するほうが正しい。**

### 4a-4. 禁止事項

- 取得と同時にパース・正規化を行い、その結果のみを保存すること
- 失敗した取得のレコードを作成しないこと
- `fetched_at` をタイムゾーン無しで記録すること
- 時刻が不明な取得に `00:00:00` 等の値を補って記録すること（`time_precision` を用いる）
- 認証情報・トークン・接続文字列を `fetch_record` に転記すること
- 本契約に適合しない収集器を本番パイプラインへ追加すること

### 4a-5. 検証

`schemas/bronze_fetch_record.schema.json` による検証を CI で実行する。

- 全収集器は、単体テストにおいて最低1件の `fetch_record` を生成し、スキーマ検証を通過すること
- スキーマ検証に失敗するコミットはマージしない
- 本契約の散文規定と JSON Schema が矛盾する場合、**散文規定を正とし、スキーマを修正する**

### 4a-6. 契約の改訂

本契約は改訂されうる。改訂時は以下に従う。

1. `bronze_contract_version` をインクリメントする
2. 改訂日以降に取得したレコードのみ新バージョンを記録する
3. **既存レコードのバージョン表記を書き換えない**
4. 旧バージョン下で取得したレコードを、新要件に適合させるための遡及的な値の補完を行わない
5. rules リポジトリで新しい `git tag` を打ち、各リポジトリの submodule を更新する

**理由**: どの規則下で取得されたかがデータ側に記録されていなければ、40年分のデータの品質保証ができない。遡及的な補完は、記録されなかった事実を捏造することに等しい。

### 4a-7. `CLAUDE_CORE.md` 掲載用要約

以下を `CLAUDE_CORE.md` に転記する（転記済み）。

```markdown
### Bronze取得契約（§4a 必読）
外部データを取得するコードを書く前に、必ず COMMON_RULES.md §4a を view すること。
- 取得1回ごとに fetch_id（UUIDv7）を発番。意味を埋め込んだIDを主キーにしない
- デコード前のバイト列を無加工で保存し、sha256 を記録する
- URL / status / 全レスポンスヘッダ / リダイレクト履歴 / TZ付き取得時刻 / 収集器バージョンを記録
- robots.txt と利用規約を対象データと同時に取得・保存する
- 失敗と非巡回期間も記録する。レコードを作らないことは禁止
本契約に適合しない収集器は、本番パイプラインに追加してはならない。
```

---

## 5. パイプライン規約

### 5-1. 分離の三原則（MUST）

1. **原本と抽出結果を分離する**
   `source_snapshots`（原本＋SHA256）と `raw_events` / `events` を別テーブルに保つ。原本を後から再パースできること。
2. **取得値と人手補正を分離する**
   `events`（取得値）と `event_overrides`（人手補正）を分け、`effective_events` ビューで `coalesce` して提供する。人手補正が取得値を破壊してはならない。
3. **候補と確定を分離する**
   すべての自動抽出は「候補（`pending`）」として入り、承認を経て確定する。

### 5-2. 消滅の扱い

新しい取得結果に前回存在した項目が無い場合、削除ではなく `missing` を立てる。一定期間後 `archived` へ。名簿（議連ロスター等）の場合、離脱と断定せず **`unconfirmed` フラグ**とする。

### 5-3. セマンティック・チャンキング（MUST）

議事録・字幕を文字数で機械的に分割することを MUST NOT とする。

- 分割単位は **発言者ごとのコメント単位**
- 各チャンクに以下を**焼き付ける**: `person_id`、発言者名、所属政党（発言時点）、`event_id`、`theme_id`、会議名、日付、`trust_score`
- 検索時は Metadata Filtering を必ず併用する

目的は「主語の消失」の防止である。

### 5-4. バックフィル（政局ラグ対応）

政局の起点は事後にしか確定しないため、遡り適用を前提とする。

1. ドラスティックな政局イベント発生時、マイルストーンとして `theme_id` を登録
2. 過去1〜2ヶ月分のデータを LLM に再スキャンさせ、`theme_id` を動的に紐付け直す
3. 紐付けの追加は非破壊（既存の関連付けを消さない）

### 5-5. 収集マナー

- レートリミットを守る。並列度・間隔は各コレクタの設定ファイルに明記する
- User-Agent に連絡先を含める
- `robots.txt` および各サイトの利用規約を確認し、判断を `docs/sources/` に記録する
- **NHK 選挙サイトは照合（cross-check）にのみ用いる**。正本は総務省・各選管
- 議員写真の公式サイトからのホットリンクを MUST NOT（初期アバターは氏名イニシャル）

### 5-6. 政府白書の体系化

白書（防衛白書・経済財政白書・厚生労働白書・警察白書・環境白書・少子化社会対策白書 等）は、先例集・国会年表とは性質が異なる資料であるため、専用の扱いを定める。担当は `policy-data-factory`。

**性質の整理（他資料との違い）**

| 資料 | 性質 | 版の扱い |
|---|---|---|
| 先例集・国会法 | 制度そのもの。改正されない限り不変 | 改正時のみ差し替え |
| 国会年表 | 事実の蓄積。過去分は増えるのみ | 追記型 |
| **白書** | **年度ごとの政府公式見解＋統計の混合物**。年度が変わると同じテーマでも記述・評価が変化しうる | **年度版を独立文書として並存させる（上書き禁止）** |

**a. trust_layer / trust_score の扱い**

白書は行政文書ゆえ `trust_layer: L1`・`trust_score: 5` を原則とする。ただし白書の中身は二種類の性質が混在している点に注意する。

- **統計表・グラフの数値**（純然たる事実） → `trust_score: 5` のまま
- **政策的記述・自己評価の文章**（「政府としては〜と認識している」等の解釈・自己評価） → 事実としては `L1`（公文書）だが、**政府の自己演出を含む**という性質を `is_official_narrative: true` フラグで明示する。数値と文章を同列に扱わない。

> 白書のグラフ・統計は、多くの場合 e-Stat 等の一次統計を政府自身が加工・引用したものである。**白書を統計の正本として扱わず、可能な限り e-Stat 等の一次統計へ遡って参照する**（転記誤り・年度間の集計方法変更の混入を避けるため）。白書側の数値は一次統計が見つからない場合のフォールバックとする。

**b. ID・版管理**

```yaml
document_id: "WP-<省庁略称>-<年度>"   # 例: WP-BOUEI-2025（防衛白書 令和7年版）
ministry: "防衛省"
fiscal_year: 2025
theme_id: "T-defense-policy"          # 同一政策領域を年度横断で束ねる
supersedes: null                       # 白書は「差し替え」ではなく「並存」なので通常null
trust_layer: "L1"
trust_score: 5
```

年度版を**独立文書**として保持し、旧版を `archived` にしない（§1-P2 非削除の原則どおり、白書は「その年に政府がそう言った」という記録そのものに価値があるため）。

**c. Silver層での構造化方針**

- 章・節の階層構造（目次）を保持したまま Markdown 化する。章番号を捨てて平坦なテキストにしない
- 図表（グラフ・統計表）は本文と分離し、**構造化JSON**として別途保持する（軸ラベル・単位・出典・年度を含む）。図表を無理に文章化して要約に埋め込まない
- OCRが必要なのは主に旧年度（PDFがスキャン画像のもの）。近年版はテキスト層を持つPDFが多く、その場合はOCR不要でテキスト抽出のみで足りる。どちらの経路を通ったかを `extraction_method: ocr | native_text` として記録する

**d. 年度差分検知（高価値ユースケース）**

同一テーマ・同一節を年度間で diff することで、政府の立場・脅威認識・政策評価の変遷を機械的に検出できる。これは §6 の Resolution/Extraction 工程の入力として特に価値が高い（「昨年は『検討中』だった記述が今年『実施』に変わった」等の変化はそれ自体が一次情報になる）。差分結果は `theme_id` に紐づく時系列ログとして保持し、要約の中に埋没させない。

**e. アクセス**

白書自体は公開情報であるため L1 として公開可。ただし白書の記述と、Layer 4（記名取材）で得た「実際の政策決定過程」との**乖離**を照合・注釈する行為自体は L4 に分類する（白書＝政府の公式見解、L4＝それとは別の実態観測、という位置づけを混同しない）。

### 5-7. YouTube記者会見動画の体系化

官房長官会見・省庁会見等の動画は tclip（TVエッジ）と類似の性質を持つが、取得経路（YouTube字幕）と話者構造（一人の記者会見に対し複数の記者が質疑する）が異なるため、専用の扱いを定める。

**a. 対象範囲（MUST）**

対象は**官邸・省庁・政党等の公式チャンネル**が投稿した動画に限定する。第三者が転載・切り抜いたクリップは対象外とする（§7 著作権原則との整合。転載動画には権利関係の不確実性が伴う）。

**b. 取得方式と段階分け**

| フェーズ | 内容 |
|---|---|
| P0 | 字幕（自動生成含む）がある動画のみ対象。生字幕を Bronze へ保存（動画URL・タイムスタンプ付き）。Silver では文字化けやフィラーのノイズ除去のみ行い、話者は `speaker: unknown` のまま保持する |
| P1 | 官邸公式の会見要旨（テキスト）が存在する場合、それを一次資料（`trust_score: 5`）とし、YouTube動画側は補助的注釈として位置づける。質疑応答の型（「一問一答」の構造）からのルールベースによる簡易話者推定を試みる |
| P2 | 音声ベースの話者分離（diarization）、字幕が存在しない動画への文字起こし（ASR）適用 |

**c. 公式要旨との差分という価値**

官邸・省庁の公式サイトに掲載される会見要旨は多くの場合**要約**であり、言い淀み・記者との噛み合わない応酬・言い直し等は要旨側で丸められる。動画側にのみ残るこの差分は、§2-3（日本の国会構造が生む「見えない政治」）の思想と直結する取材対象になり得る。「公式要旨に無いが動画にはある発言」の検出ロジックは、§6-1 Resolution 工程の応用として位置づける。

**d. `event_id` との紐付け**

既存の `admin/ingest` の統合判定ロジック（類似イベント検出→統合 or 新規作成）をそのまま適用する。同一会見に対し複数ソース（動画・官邸要旨・報道）が存在する場合は `event_sources` に複数紐づけ、単一の `event_id` に集約する（§2-1 の発番一本化原則）。

**e. セマンティック・チャンキングとの関係**

話者分離が完了する（P2）までは、§5-3 の「発言者ごとのコメント単位」の要件を厳密には満たさない。P0/P1 段階のチャンクには `speaker_confidence: unable | low` を明示し、検索時にこの点が利用者に伝わるようにする（§6-4 の「不能」表示原則と同様の扱い）。

### 5-8. 証拠（evidence）・event_type の統一登録簿

出席・発言の根拠種別（evidence）と `events.event_type` は、admin/ingest・YouTube（§5-7）・TV放送（§5-9）を横断して同一の登録簿を参照する（MUST）。各リポジトリが個別に列挙値を持つことを禁止する。正本は `kokkai-nexus-rules`（§0-1）の JSON Schema とする。

**a. evidence 種別（信頼性の高い順）**

| 種別 | 意味 | 備考 |
|---|---|---|
| `roster` | 名簿・議事録記載 | 公的記録・最高信頼 |
| `sns_self` | 本人がSNS・ブログで報告 | 本人発信・高信頼 |
| `broadcast_telop` | 放送中のテロップ（画面上の氏名表示）による確認 | 放送局側が人物を確定させた表示。§5-9 で新設 |
| `interview` | 取材による確認 | 記名必須 |
| `photo_tag` | 写真に写っている | 誤認リスクあり |
| `broadcast_cc` | 放送の生字幕テキストに言及があるのみ（映像上の本人確認なし） | 誤変換・話者不明のリスクあり。§5-9 で新設 |
| `manual` | 入力者の判断 | 要確認フラグ付き |

同一人物への根拠（evidence）が複数種類重なるほど `confidence` が上がる設計は既存方針のとおりとする。

**b. event_type 列挙**

`plenary` / `committee` / `caucus` / `party_meeting` / `study_group` / `press` / `broadcast_program`（§5-9） / `election`（ADR-0003） / `gazette`（ADR-0003） / `cabinet_decision`（ADR-0003） / `statistics_release`（ADR-0005） / `advisory_council`（ADR-0005） / `court`（ADR-0005） / `diplomacy`（ADR-0005） / `policy_schedule`（ADR-0005） / `other`

- `press`: 記者会見（一人の登壇者・記者からの質疑という構造）
- `broadcast_program`: 討論番組・報道特番等（複数の政党代表・出演者が並ぶ構造）。両者は構造が異なるため、混同しない。
- `election`: 選挙の**投開票**という単一事象。**公示・告示は官報に載る公的行為であり `gazette` で表現する**。同一選挙の公示と投開票は別イベントとし、同じ `theme_id` で束ねる。
- `gazette`: 官報における公布・公示。`trust_layer` L1 / `trust_score` 5（§3-2・§3-3）。
- `cabinet_decision`: 閣議1回（定例・臨時・持ち回り・繰上げ・初閣議）。**会議体の開催**を指し、個々の「◯◯を決定した」は案件として `event_links` にぶら下げる。
- `statistics_release`: 統計・指標の公表（GDP速報・短観・貿易統計・月例経済報告）。政策判断の入力となる定期公表。「短観の翌週に金融政策決定会合」のような時系列の連鎖を追うために独立させる。
- `advisory_council`: 行政の諮問機関（審議会・分科会・部会・懇談会・検討会）。**国会の `committee` とは別**。
- `court`: 判決・公判。選挙訴訟・行政訴訟を含む。
- `diplomacy`: 首脳・閣僚級の外交日程（首脳会談・外相会談・G7/G20・国際会合）。
- `policy_schedule`: 政策・政局に関わるが上記のいずれにも確定できないもの。**`other` の受け皿を分離**し、`other` を「分類の失敗」の指標として使えるようにする。

**c. 粒度の線引き（MUST・ADR-0003）**

`events` は「**会議体・事象の1回**」を表す。その中身（案件・候補者・公布事項）を `events` の行にしてはならない。中身は `event_links` にぶら下げる。背骨を明細で肥大させると、「1回の閣議」「1つの選挙」という事象そのものが表現できなくなり、`source_count` 等の集計単位も壊れる。

| 対象 | events | event_links |
|---|---|---|
| 閣議 | 閣議1回（開催日＋種別） | 案件（`link_role='agenda'`） |
| 選挙 | 1選挙（投開票） | 候補者・市町村別得票 |
| 官報 | 1号 | 公布事項 |

**d. 本列挙に統合しないもの（ADR-0003 §2-3）**

`pm_activities.event_type`（`面会` / `会議` / `職務・公務` / `移動` / `メディア` / `その他`）は**首相の行動分類**であり、本列挙（会議体・事象の構造分類）とは目的が異なる。統合しない。ドメイン固有列として各テーブルに保持し、背骨に載せる際は `events.event_type` を別途与える。

> 混ぜた場合の弊害: `plenary` と `移動` が同一の列挙に並び、会議体分類としても行動分類としても集計できなくなる。

**e. 背骨の収録範囲（MUST・ADR-0005）**

`events`（背骨）に収録するのは、**政策・政局・選挙・行政・司法に関わる事象**に限る。
判断に迷う場合の基準は §1-P7「後世の検証に必要か」とする。

**収集層と背骨で、絞る位置を分ける（MUST）**

| 層 | 方針 |
|---|---|
| 収集（Bronze・名寄せ） | **網羅的に取得する。ここで絞らない**。一度取り損ねた期間は事後に復元できない（§4a） |
| 背骨への昇格 | **選別する**。ソースごとに「どの部分を背骨に上げるか」を昇格の前に決める |

新しいソースを背骨へ接続する前に、この線引きを明示すること。実装してから考えると、
範囲外のレコードが背骨に混入したまま運用が始まる。

**範囲外レコードの扱い**

物理削除しない（§1-P2）。`is_hidden = true` ＋ `hide_reason` に判断根拠を記録する。
`hide_reason` を空のまま非表示にすることを MUST NOT とする（理由なき非表示は検証できない）。
範囲の判断が後日変われば `is_hidden = false` で戻せる。

> 実例（ADR-0005）: 時事通信の総合取材カレンダーを選別せずに投入した結果、
> F1のレース日程・映画祭・株主総会が背骨に入った。収集は正しかったが、昇格の選別が無かった。

### 5-9. テレビ放送TSデータの体系化

自局録画（tclip・TVエッジ環境）による地上波・BS報道番組（日曜討論等）のTS（トランスポートストリーム）データを対象とする。担当は `tclip`。

**a. 番組詳細（EPG）データとの統合**

番組・出演者マスタ（`programs` / `talents` / `program_talent_appearances`、T-Clip DB）は、既存の `person_id` 一元化原則（§2-1）に従う。**`talents` レコードが政治家である場合、`talent_id` を独立採番せず、必ず既存の `person_id` に紐付ける**（MUST）。ジャーナリスト・コメンテーター等、政治家でない出演者に限り `talent_id`（tclip固有）を許容する。当該人物が後に立候補する等して政治家になった場合は、`person_id` への紐付けへ移行する。

**b. パイプライン**

TS を音声・映像・字幕多重に分離したうえで、以下を抽出する。

- **ARIB字幕（生字幕）のデコード**: 日本のデジタル放送は ARIB STD-B24 規格の字幕多重を持つ。既存のtclip「生字幕処理」はこれをデコードする形になる
- **テロップOCR**: 発言者名が画面上のテロップ（名前・肩書表示）として提示される瞬間をフレーム解析し、OCRで抽出する。**テロップは放送局が既に人物を確定させた表示であるため、字幕テキストのみからの話者推定より信頼度が高い**（§5-8-a の `broadcast_telop`）
- **スクリーンショット生成**: 話者交代の検知時、または一定間隔で自動生成し、`event_sources` の写真情報として保持する

**c. 話者同定の優先順位（MUST）**

テロップOCRで発言者を確認できた区間は `broadcast_telop` として記録し、字幕テキストのみで発言者が確認できない区間は `broadcast_cc` として低い信頼度のまま記録する。テロップ未確認の区間について話者を推測で割り当てることを MUST NOT とする（§1-P3: 確認できないものを確認済みとして出さない）。§5-3 のセマンティック・チャンキング要件は、`broadcast_telop` で確認された区間から優先的に満たす。

**d. trust_layer / trust_score**

番組自体は `trust_layer: L2`（公開・動的、報道）に分類する。ただしテロップで話者が確定した逐語発言は、政治家自身の一次的な発言であることを踏まえ `trust_score: 4`（政党一次情報相当）として扱ってよい。一方、番組の構成・編集判断（誰をどれだけ映すか、討論の切り取り方等）はそれ自体が放送局の解釈を含むため、事実として同列に扱わない（§3-1: 軸の混同禁止）。

**e. event_id 紐付け**

`event_type: broadcast_program`（§5-8-b）を用いる。同一番組内の複数出演者は `event_attendees` で管理し、既存 admin/ingest の統合判定ロジック（類似イベント検出→統合 or 新規作成）をそのまま適用する（§2-1 の発番一本化原則）。

**f. 著作権・配信制約（MUST）**

TS原本（Bronze層）は内部の検証・分析専用アーカイブとし、**非公開**とする。対外的な製品（Kokkai Nexus / PoliData 双方）が提供できるのは、構造化された事実（誰が・いつ・何を発言したか）と、著作権法の引用要件を満たす短い引用のみである。動画・音声そのものの外部配信、スクリーンショットの一般公開、字幕全文の転載は行わない（§4-1 の Bronze層方針、§7 の事実と表現の分離原則の直接適用）。

**g. 物理バックアップとの関係**

TS原本は放送終了後、二度と同じ形では取得できない再取得不可能な資産である。§4-4 の 3-2-1 ルールの適用対象として最優先に扱う。

---

## 6. AI / LLM 利用規約（内部処理）

### 6-1. 標準パイプライン（4段階抽出モデル）

ナレッジグラフ構築を伴う処理（GraphRAG）は、以下の4段階＋評価フィードバックループを標準形とする。

> 出典についての注記: 本節は Anthropic 社が公開する Knowledge Graph Cookbook・Building Effective AI Agents 等の知見を土台に、第三者が体系化した研究ノートの構成を参考にしたものであり、**Anthropic 公式の規格ではない**。採用にあたっては自社ドメイン（政治・政策）向けに検証したうえで採否を判断し、権威づけのために「Anthropic公式」と称してはならない。

```
[Documents] → [1. Extraction] → [2. Resolution] → [3. Assembly] → [4. Querying] → [回答]
                    ▲                                                    │
                    └─────────────── Evaluation Feedback Loop ───────────┘
```

| 段階 | 処理内容 | 想定モデル | 理由 |
|---|---|---|---|
| **1. Extraction** | 未構造化文書から S-P-O トリプルおよびエンティティを抽出。ドキュメント内の事実のみを機械的に抽出し、事前学習知識で補完しない | 軽量高速モデル（Haiku クラス） | 大量処理・低コストが優先されるため |
| **2. Resolution** | 表記揺れ・同義語の統合（例：「高市大臣」→「高市早苗」/ `person_id` への解決）。単純文字列一致ではなく型・文脈を判断する | 高度推論モデル（Sonnet クラス以上） | 政治文脈特有の同一性判断が必要なため |
| **3. Assembly** | 解決済みノード・エッジ・要約プロファイルを統合し、多重有向グラフ（MultiDiGraph）と各エンティティのプロファイル要約を構築 | — | グラフ構造化処理。以降の検索・推論の基盤となる |
| **4. Querying** | クエリに対しサブグラフ抽出とベクトル検索を複合実行し、マルチホップ推論のうえ根拠（`event_id`/`source_id`）付きで回答生成 | 高度推論モデル（Sonnet クラス以上） | 複数関係を跨ぐ推論と、引用の正確性が要求されるため |

**評価フィードバックループ**: Querying の結果評価（RAGAS 指標＝Faithfulness / Answer Relevance、および Ground Truth テスト）に基づき、Extraction/Resolution 側のプロンプトを調整する。以下の3段階で対応する。

1. **プロンプト調整** — ハルシネーションやトリプル抽出漏れの検知時、ネガティブ制約・Few-shot例を最適化
2. **スコアラー再実行** — 精度低下が発生した段階を自動特定
3. **モデルの再学習** — 検証済みの高品質抽出ペアを蓄積し、Haiku クラスの抽出モデルをファインチューニング（低レイテンシと精度向上の両立）

**本システムへの適用における注意（§1-P3・P5 との整合）**

- Extraction/Resolution/Assembly は§1-P3（検証可能性）を満たす必要がある。抽出されたトリプルは必ず元ドキュメントの `source_id`/`snapshot_id` に逆引きできること
- Assembly で生成されるグラフは Pinecone（意味検索）と併存するハイブリッド構成とし、§4-2 の役割分担（RDB=確定事実、Vector=派生）を踏襲する。グラフ自体も**再構築可能な派生物**として扱い、正本は Silver/Gold 層の構造化データとする
- Resolution 工程での同一性統合結果を Layer 4/5 の確定データへ直接反映することを MUST NOT（§1-P5・§6-6 のとおり、確定は記名の人間が行う）

### 6-2. 実行基盤としての Dynamic Workflows（Claude Code）

§6-1 の4段階パイプラインを実装する際、Claude Code の **Dynamic Workflows**（2026年5月28日公開、Anthropic公式機能）を実行基盤として利用できる。これは Claude が JavaScript のオーケストレーションスクリプトを自ら書き、数十〜数百のサブエージェントを並列に起動する機能であり、オーケストレーション自体はコードであるためトークンを消費しない。

> **出典の切り分けについて**: 本節で述べる Dynamic Workflows 自体は `code.claude.com/docs/en/workflows` および Anthropic公式ブログで確認できる実在の機能である。一方、参考として共有された X（旧Twitter）上の「14-step roadmap」記事、および「Anthropicのシニアエンジニアが12ページのPDFを公開した」という投稿は、**第三者による技術解説・伝聞であり、Anthropic公式の一次情報ではない**（前者は投稿者自身が集客用Substackへの誘導を兼ねており、GPTZeroにより「AI Detected」と判定されている）。技術的な記述（`agent()`/`parallel()`/`pipeline()` の挙動等）自体は公式ドキュメントの内容と整合しているため参考にする価値はあるが、「誰が書いたか」「何が公式見解か」を本ファイルでは区別して扱う。本プロジェクトの §1-P3（検証可能性）を、ドキュメント自身にも適用する。

**a. 基本プリミティブ**

| プリミティブ | 挙動 | 本プロジェクトでの用途 |
|---|---|---|
| `agent()` | サブエージェントを1体起動。JSON Schema を渡すと構造化出力を強制できる | Extraction段階で1ドキュメント＝1エージェント。S-P-Oトリプルのスキーマを§2-1のID体系に合わせて定義する |
| `parallel()` | **バリア**。全サブエージェントの完了を待ってから次段へ進む。1体が失敗しても `null` を返し全体は止まらない | 複数ソースを横断してデデュープ・ランキングする工程（例: 白書の年度差分検知の集約、複数SNS投稿の統合判定）に限定して使う |
| `pipeline()` | バリアなし。各アイテムが独立して全段を流れる | **既定はこちら**。ドキュメント単位のExtractionなど、アイテム間に依存が無い処理はバリアを張らない |

**b. §6-1 との対応**

- **Extraction** → `pipeline()` で1ドキュメントずつ流す（Bronze層の各原本は独立して処理できるため、バリアで待たせる理由がない）
- **Resolution** → 表記揺れの統合はソース横断の判断を要するため `parallel()` のバリアで全結果を集めてから実行する
- **Assembly** → コード（プレーンJS）でのグラフ構築が中心。「結合するだけ」の工程にエージェントを使わない（§6-1 の「エッジは無料」の原則と同じ）
- **Querying** → サブグラフ抽出後の推論部分のみエージェントを使う

**c. モデル階層の徹底（§6-1 モデル表との整合）**

サブエージェントは既定でセッションのモデルを継承するため、何も指定しなければ全ノードが高コストモデルで実行される。`agent()` 呼び出しごとに `model` オプションを明示し、Extraction 等の反復的なノードは軽量モデル（Haiku クラス）へ、Resolution/Assembly/Querying 等の判断が必要なノードは高度推論モデル（Sonnet クラス以上）へ、明示的に振り分けること（MUST）。振り分け忘れは §6-1 のコスト設計そのものを無効化する。

**d. 検証パターンと§1-P5の関係**

Dynamic Workflows は「複数の懐疑的エージェントで多数決を取る」検証パターン（adversarial verify / judge panel）を提供する。これは §5-1（原本と抽出結果の分離）や事実抽出の精度向上には有効に使える。ただし、**このAI検証パターンをもって Layer 4/5 の確定（記名の人間による承認）を代替してはならない**（§1-P5）。AI検証はあくまで「Extraction/Resolutionの候補精度を上げる」段階に留め、公開データとしての確定は別工程とする。

**e. 未知範囲の探索（loop-until-dry）**

終了条件が事前にわからない探索（例: Nexus 関係性検出における「議連の隠れた繋がりをどこまで洗い出すか」）には、連続K回何も新規発見が無くなるまで繰り返す `loop-until-dry` パターンが使える。ただし既発見（`seen`）に対して重複判定を行い、確定済み（`confirmed`）だけと比較しないこと。確定済みとだけ比較すると、一度却下した候補が毎回再浮上し、ループが収束しなくなる。

**f. 並行書き込みの隔離**

複数エージェントが同時にファイル（Silver層Markdown等）へ書き込む場合、衝突を避けるため作業ディレクトリを分離した上でマージする設計とする。単一の議員・単一のイベントへの同時書き込みが起こり得る箇所（例: 一括OCRバッチ、ロスター一括取込）でのみ適用し、すべての処理に一律で課さない。

**g. 適用範囲の限定**

Dynamic Workflows は Claude Code（CLI/Desktop/VS Code拡張、Max/Team/Enterprise、またはAPI経由）に紐づく実行基盤であり、本番のNode.js/Expressサーバー上で常時稼働させる仕組みではない。**開発時のバッチ処理・大規模な一括構造化・調査タスク**（例: 白書OCRの一括投入、政局バックフィルの再スキャン）に用途を限定し、本番サービングのリクエストパスに組み込まない。

### 6-3. モデル運用ルール

コストと精度のトレードオフはリポジトリごとに計測し、`docs/report/` に記録する。§6-1 の役割分担を初期値とし、実測に基づき見直す。

### 6-4. 出力の必須属性

LLM 由来の値は、以下を伴わずに保存してはならない。

```yaml
is_ai_generated: true
model: "<model-id>"
confidence: high | medium | low | unable
basis:            # 根拠の実体
  - source_id / snapshot_id / timestamp
generated_at: <ISO8601>
```

根拠データ不足時は推論を強制せず **`unable`（推論不能）** と表示する。推論を無理に埋めることは信頼性を毀損する。

### 6-5. AI推論タグの表示原則（対外）

- 事実の断定ではないことを明示する
- 根拠（参照データ種別・件数・確認日時）を必ず併記する
- 信頼度を「高・中・低・不能」の4段階で表示する
- 政策的立場の優劣を評価しない（分類であって推奨ではない）

### 6-6. Human-in-the-Loop エキスパート評価

1. AI がデータ間の矛盾・空白（ミッシングリンク）を検知
2. 専門家への質問を自動生成
3. 非同期UI（admin-ui / Slack 等）で専門家が**記名で**回答
4. LLM が回答を Silver層 Markdown ＋ Gold層 JSON に構造化
5. `event_id` / `theme_id` で既存データへ名寄せ

**制約**: 4→5 の自動昇格は §1-P5 と緊張する。構造化までを自動とし、**確定は記名の人間が行う**。第三者論評（`third_party_commentary`）と自前取材（L4）は峻別し、混在させない。

---

## 7. 法務・コンプライアンス規約

| 項目 | 規定 |
|---|---|
| 公的記録 | 著作権法13条により自由利用可（L1） |
| 報道記事 | 著作権法32条の引用要件を遵守。**見出し＋リンクのみ**、本文転載 MUST NOT |
| 議員SNS | 要約＋出典リンクのみ |
| 手記・回顧録 | 引用の範囲内。書誌情報（書名・著者・発行年・出版社・頁）を MUST |
| 選挙期間中 | 公示（告示）日〜投開票日は、特定候補者に関する**新規AI推論タグ付与および既存タグ更新を停止**（利用規約 第11条） |
| AI学習利用 | 自社コンテンツの第三者による学習利用は許諾制。技術的保護手段（API認証・レートリミット）を実装する |
| 個人情報 | 記録対象は原則として公職者の**公的活動**に限定 |

新機能の設計時、法務要件は「後付けのチェック」ではなく**要件定義の一部**として扱う。

---

## 8. リポジトリ責務

### 8-1. Kokkai Nexus 系（市民向け・記録基盤）

| リポジトリ | 責務 |
|---|---|
| `kokkai-giin-profiles` | 静的プロフィールデータ |
| `kokkai-nexus-core` | 主データ処理（NDL API・スクレイパー・パイプライン） |
| `KokkaiNexus` | アプリ本体（React/Vite + Node.js/Express + Supabase）。**管理画面は `/admin` に全統合** |
| `kokkai-nexus-election` | 選挙データ |
| `kokkai-nexus-pipeline` | データ変換・加工 |
| `kokkai-nexus-master` | 検証済みマスタ（Git Submodule）。**本ファイルの正本置き場** |

### 8-2. PoliData 系（商用RAG・インテリジェンス）

| リポジトリ | 責務 |
|---|---|
| `PoliData_Development` | ドメイン別モジュール（event / news / policy / giin / election）、API・フロント |
| `policy-rag-poc` | RAG基盤（Pinecone・GraphRAG・FastAPI・評価ループ） |
| `openclaw-news-pipeline` | 政党・官邸・省庁・社説の自動巡回収集。**原本は不変（Immutable）**、加工しない |
| `policy-data-factory`（新設予定） | 先例集・国会年表等の固定PDFのOCR・構造化 |
| `tclip` / TVエッジ | 番組・出演ログ・生字幕・スクリーンショット生成 |
| `Jiji_schedule` | 政策日程の収集・管理（時事通信・省庁・各党） |

### 8-3. 越境ルール

- 収集層は加工しない。加工層は収集しない。RAG層は原本を書き換えない。
- リポジトリ間の受け渡しは **Silver層 Markdown / Gold層 JSON** に限る。生スクリプトの相互直呼び出しを MUST NOT
- 全データのメタデータは中央資料目録 `document_catalog.yaml`（ID接頭辞・タイトル・発行元・版・`trust_score`・ホット/コールド）で一元定義する

---

## 9. ドキュメント・コード規約

### 9-1. ディレクトリ

```
<repo>/
├── CLAUDE.md                  ← 本ルールへの参照＋当リポ固有の差分のみ
├── docs/
│   ├── concept/               設計思想
│   ├── architecture/          構成図・データフロー
│   ├── decisions/             ADR-NNNN-*.md（意思決定記録）
│   ├── plan/                  実装計画
│   ├── report/                セッションサマリー・計測結果
│   └── sources/               収集対象ごとの規約確認記録
├── src/
└── data/                      （GCS マウント or ローカルキャッシュ）
```

### 9-2. 文書の書き方

- 設計文書は **である調**（文語体）で記述する
- 「決まったこと」と「未決」を必ず分節する。曖昧なまま断定しない
- セッション終了時、Cursor / Claude 継承用に `docs/report/cursor_session_summary.md` を更新する

### 9-3. コード

- シークレットをリポジトリに置くことを MUST NOT。`.env` ＋ `.env.example` を用いる
- 検証用スクリプト（`tmp_*.py` 等）は `core/` へ昇格させる前提で書き、放置しない
- 生成コードは**省略しない**（`# ...以下同様` のような省略を MUST NOT）

### 9-4. AIエージェントへの指示（Claude / Cursor）

**a. 二層構成（MUST）**

`COMMON_RULES.md`（本文書）は人間向けの完全な規範であり、分量の都合上そのまま Claude Code の `CLAUDE.md` に `@import` してはならない。Claude Code公式ドキュメントによれば、`CLAUDE.md` は200行を超えるとコンテキスト消費が増え、指示への追従性が下がる。

代わりに、`kokkai-nexus-rules`（§0-1）に **`CLAUDE_CORE.md`**（100行程度のダイジェスト）を置き、各リポジトリはこちらを `@import` する。`CLAUDE_CORE.md` には P1〜P7・ID規約・trust_layer/scoreの混同禁止・パイプラインの基本形のみを収め、詳細が必要な章番号への参照表を末尾に置く。AIエージェントは必要な章だけを `COMMON_RULES.md` から `view` で読みに行く（全文常時ロードではなく、必要時アクセス）。

**b. 各リポジトリの `CLAUDE.md` 構成（テンプレート: `kokkai-nexus-rules/CLAUDE.md.template`）**

```markdown
# <リポジトリ名> — Claude Code 設定

@.rules/CLAUDE_CORE.md

## 本リポジトリの担当範囲
（§8-1/8-2 の該当行を転記）

## 本リポジトリ固有の差分
（COMMON_RULESとの差分のみ）
```

- `.rules/` は `kokkai-nexus-rules` の Git Submodule。参照は §0-1 のとおり `git tag` にピン留めする
- 初回セッションで外部インポートの承認ダイアログが出るため、各開発者は許可する
- 見出しレベルの実装上の注意: `@import` されたファイルの見出しレベルは自動調整されない。`CLAUDE_CORE.md` 側は `##` から書き始め、リポジトリ側の `#` タイトルの子として自然に収まるようにする

**c. 条件付きロード（`.claude/rules/*.md`）は現時点で採用しない**

Claude Code には frontmatter（`globs:`）でファイル種別ごとに条件付きロードする仕組みがあるが、リポジトリ間で共有するには submodule 経由のシンボリックリンクが必要になり、Windows環境（本プロジェクトの開発環境の一部）でのシンボリックリンクの脆弱さを踏まえ、**現時点では採用しない**（§11 未決事項へ）。当面は `CLAUDE_CORE.md` の参照表と、AIエージェントへの「必要な章を`view`で読みに行け」という指示で代替する。

**d. 各リポジトリの `CLAUDE.md` 冒頭の宣言文**

> 本リポジトリで生成するコードは、`COMMON_RULES.md` の思想・ディレクトリ構造・データ処理設計に 100% 合致すること。ルールに反する実装が必要と判断した場合は、実装せず ADR の起票を提案すること。

---

## 10. 実装フェーズ規約

| フェーズ | 内容 |
|---|---|
| P0 | 手動CRUD、L1 自動収集 |
| P1 | イベント連携、AI支援（人間レビュー前提） |
| P2 | 公開UI、時系列可視化、AI相関分析 |

**優先順位の原則**: 出力層（RAG回答品質）の最適化より、基盤層（Nexus 関係性検出、`event_id` の串刺し）の完成が高レバレッジである。対話AIの品質はデータ完全性に上限を規定される。

---

## 11. 未決事項（Open Questions）

本章は「まだ決まっていない」ことを明示するための欄である。決定次第 ADR 化して本文へ移す。

**A. 共通ルールの物理的な共有方法 → 解決済み（v1.3）**
独立リポジトリ `kokkai-nexus-rules` を新設し、全リポジトリが Submodule として参照する（§0-1）。バージョンは `git tag` でピン留めする。

**B. 二系統ブランドとデータ流用範囲 → 部分決定（要最終確認）**
§3-6 に暫定決定を記載した。要点：正本は単一DB／同意フラグは**デフォルト非許諾**／DBロールのRLSで一次防御／PoliData側の出力は機能レベルでロビイング支援を実装しない。**デフォルト値を`false`とした点は提示された案からの変更であり、事業判断として Souichirou の最終確認を要する。**

**C. `event_id` / `theme_id` の具体的な採番形式 → 解決済み（v1.3）**
`EV-<YYYYMMDD>-<4桁連番>` / `TH-<SLUG>-<3桁連番>` を採用（§2-1）。ID文字列のパースをクエリロジックに使わないこと、採番はPostgres側でアトミックに行うことを条件として付記した。

**D. Human-in-the-Loop の昇格境界**
専門家回答をどこまで自動構造化し、どこから人間確定とするか。§6-6 の「4→5」の線引き。

**E. GraphRAG と Pinecone の検索ルーティング**
「全体像の把握」と「ピンポイント検索」でのルーター設計。

**F. 評価メトリクス**
政治的公平性（複数政党の主張の網羅）と時系列矛盾検知の指標化。

**G. 先例集・年表の構造化精度**
図表・議事進行フローをどこまで JSON/Markdown で保持するか。

**H. 白書の年度差分検知の実装方式**
同一節を年度間で diff する処理を Extraction 段階の一部とするか、独立バッチとして別工程化するか。差分結果を `theme_id` の時系列ログとしてどこまで自動要約するか（§5-6-d）。

**I. GraphRAG（Assembly段階）のグラフ保存先**
多重有向グラフ（MultiDiGraph）の永続化先をどこに置くか（Supabase 上にエッジテーブルとして持つか、専用グラフDBを導入するか）。§4-2 の「RDB=確定事実／Vector=派生」の二分法に、グラフという第三の派生物をどう位置づけるかが論点（§6-1）。

**J. Dynamic Workflows の適用範囲の実測**
§6-2 の Extraction/Resolution 振り分けが、実際のコスト・レイテンシでどの程度効くかは未計測。特に白書OCRバッチや政局バックフィルの再スキャンなど「探索範囲が大きいがバースト的な」タスクにどこまで委ねるか、`docs/report/` への計測結果蓄積を待って判断する。

**K. `.claude/rules/*.md` の条件付きロード導入可否**
§9-4-c で当面見送ったが、Windows以外の開発環境が増えた場合、またはリポジトリ数が増えて`CLAUDE_CORE.md`の参照表だけでは足りなくなった場合は再検討する。

---

## 12. 変更履歴

| 版 | 日付 | 変更内容 |
|---|---|---|
| v1.0 | 2026-07-24 | 初版。既存の設計思想文書・利用規約・グランドデザイン報告書・セッションサマリーを統合 |
| v1.1 | 2026-07-24 | §5-6「政府白書の体系化」を新設。§6-1「標準パイプライン（4段階抽出モデル）」を新設し、GraphRAG 4段階（Extraction/Resolution/Assembly/Querying）＋評価フィードバックループを標準形として明記。未決事項に H・I を追加 |
| v1.2 | 2026-07-24 | §6-2「実行基盤としての Dynamic Workflows（Claude Code）」を新設。Anthropic公式機能（2026-05-28公開）としての Dynamic Workflows の実装パターン（`agent()`/`parallel()`/`pipeline()`、モデル階層振り分け、adversarial verify、loop-until-dry、worktree隔離）を §6-1 の4段階パイプラインへの実装オプションとして整理。参考共有された第三者記事（X上の技術解説投稿）については、公式一次情報と第三者による伝聞・技術解説を区別する注記を付記。未決事項に J を追加。§6章内の節番号を 6-1〜6-6 に採番し直し |
| v1.3 | 2026-07-24 | §5-7「YouTube記者会見動画の体系化」を新設（P0〜P2の段階分け、公式チャンネル限定、話者分離未完了時の`speaker_confidence`明示）。未決事項 A（共通ルール共有方法）・C（ID採番形式）を解決済みへ更新：§0-1にリポジトリ`kokkai-nexus-rules`新設と`git tag`ピン留めを追記、§2-1に`EV-<YYYYMMDD>-<4桁連番>`/`TH-<SLUG>-<3桁連番>`形式と採番時の注意点（ID文字列パース禁止・Postgresアトミック採番・スラッグ registry）を追記。未決事項 B（二系統ブランドのL4流用）は §3-6 として部分決定を追記（正本単一DB・同意フラグのデフォルト非許諾・RLSによるDB層強制・PoliData出力への機能レベル制約）。デフォルト値を`false`とした点は提示案からの変更であり、事業判断としてSouichirouの最終確認待ち |
| v1.4 | 2026-07-24 | §4-4「物理バックアップ（事業継続計画・BCP）」を新設し、3-2-1ルールを正式に規約化。§5-8「証拠（evidence）・event_typeの統一登録簿」を新設し、既存の`roster`/`sns_self`/`interview`/`photo_tag`/`manual`に加え`broadcast_telop`/`broadcast_cc`を追加、`event_type`に`broadcast_program`を追加。§5-9「テレビ放送TSデータの体系化」を新設（tclip担当、ARIB字幕デコード・テロップOCRによる話者同定優先順位、`talents`テーブルの`person_id`一元化、trust_score 4の適用条件、著作権上の非公開原則、3-2-1バックアップとの関係） |
| v1.5 | 2026-07-24 | §9-4「AIエージェントへの指示」を全面改訂。`COMMON_RULES.md`全文をClaude Codeの`CLAUDE.md`へ`@import`することを禁止し（200行超過によるコンテキスト消費・指示追従性低下という公式ドキュメントの制約に基づく）、100行程度のダイジェスト`CLAUDE_CORE.md`と各リポ用`CLAUDE.md.template`を新設。`kokkai-nexus-rules`の収録物（§0-1）を更新。`.claude/rules/*.md`の条件付きロードは当面見送り、未決事項Kとして記録 |
| v1.6 | 2026-08-03 | §4a「Bronze 取得契約」を新設（§5系より前の位置に配置）。P2/P3の取得境界面における実装規定として、R1〜R5（不透明ID・取得文脈・デコード前バイト列・権利スナップショット・欠損記録）を明文化。`schemas/bronze_fetch_record.schema.json`（JSON Schema Draft 2020-12）を新設しCI検証を規定。`CLAUDE_CORE.md` にBronze取得契約要約ブロックを追記。Bronze取得契約バージョン v1.0 発効 |
| v1.7 | 2026-09-08 | §5-8-b の `event_type` 列挙に `election` / `gazette` / `cabinet_decision` を追加（ADR-0003）。§5-8-c「粒度の線引き」を新設し、events は会議体・事象の1回を表し明細は `event_links` にぶら下げることを MUST 化。§5-8-d で `pm_activities.event_type`（行動分類）を本列挙に統合しない旨を明記。§2-1c「外部システムの安定キー」を新設し、`<system>_external_ids` 対応表と決定論的キーの使用を MUST 化（ADR-0004。冪等な再構築を行うパイプラインの連番IDは実行のたびに別レコードを指しうるため）。§4-2 に収集リポ内 SQLite（名寄せ作業場・正本ではない）の位置づけを追記。`schemas/event_type.enum.json` を更新 |
| v1.8 | 2026-09-08 | §5-8-b に `statistics_release` / `advisory_council` / `court` / `diplomacy` / `policy_schedule` を追加（ADR-0005）。`policy_schedule` を設けて `other` の受け皿を分離し、`other` を「分類の失敗」の指標として使えるようにした。**§5-8-e「背骨の収録範囲」を新設**し、収集層は網羅的・背骨への昇格時に選別するという線引きを MUST 化。範囲外レコードは物理削除せず `is_hidden` ＋ `hide_reason`（理由なき非表示は MUST NOT）。`schemas/event_type.enum.json` を更新 |
