# <リポジトリ名> — Claude Code 設定

@.rules/CLAUDE_CORE.md

> 上記1行が `kokkai-nexus-rules` サブモジュール内の中核規約をインポートする。矛盾する指示があった場合、`.rules/COMMON_RULES.md` を優先する。初回セッションでは外部インポートの承認ダイアログが出るので許可すること。

## 本リポジトリの担当範囲

<!-- COMMON_RULES.md §8-1/§8-2 の該当行をそのまま転記する -->

## 本リポジトリ固有の差分（COMMON_RULESとの差分のみ記述）

<!-- 例:
- 本リポジトリは収集専用であり、加工・構造化は一切行わない（§8-3 越境ルール）
- 出力形式: Markdown（Bronze層相当）。Silver/Gold層への変換は別リポジトリの責務
-->

## よく使うコマンド

<!-- 例: python -m scrapers.run --target X / npm run dev 等、正確な文字列で -->

## このリポジトリで特に注意すべき落とし穴

<!-- COMMON_RULESに書くほど一般的ではないが、このリポ特有の事故りやすいポイント -->

---

<!--
=====================================================================
以下は記入例（policy-rag-poc 用）。実ファイルでは削除して上を埋めること。
=====================================================================

## 本リポジトリの担当範囲
RAG基盤（Pinecone・GraphRAG・FastAPI・評価ループ）。全データ（動的＋静的）の
一括前処理（preprocess.py）、Knowledge Graph（S-P-Oトリプル）構築、
3段階自動評価（Tier 1/2/3）を担う。

## 本リポジトリ固有の差分
- GraphRAGパイプラインの実装本体はここに置く（COMMON_RULES §6-1 の Extraction/
  Resolution/Assembly/Querying を実装する場所）
- Assembly段階で生成するグラフの永続化先は未決（COMMON_RULES §11 未決I）。
  現状は本リポジトリ内のローカルグラフとして保持し、Supabaseへの正式な
  永続化方式が決まるまで「再構築可能な派生物」として扱うこと
- Dynamic Workflows（§6-2）を用いる場合、Extraction段階の agent() 呼び出しは
  必ず軽量モデルを明示指定すること（既定のセッションモデル継承のまま
  流さない）

=====================================================================
以下は記入例（openclaw-news-pipeline 用）。実ファイルでは削除して上を埋めること。
=====================================================================

## 本リポジトリの担当範囲
政党・官邸・府省・社説の自動巡回収集（日次・時次）。原本データ（raw /
clippings_*）は不変（Immutable）で蓄積・保存し、加工処理は行わない
（Single Source of Truth、COMMON_RULES §8-3 越境ルール）。

## 本リポジトリ固有の差分
- 本リポジトリが生成してよいのは Bronze層相当の生データのみ。ノイズ除去・
  構造化・LLMによる要約は一切行わない（隣接リポジトリの責務）
- 収集マナー（COMMON_RULES §5-5）を厳守: レートリミット・User-Agentへの
  連絡先明記・robots.txt確認の結果を `docs/sources/` に記録する
- 議員写真の公式サイトからのホットリンクは MUST NOT
- 選挙期間中の巡回対象・頻度変更ルールがあれば、ここに明記する（現状特記事項なし）
-->
